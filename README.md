Before implementing anything, please do a risk analysis first:

	1.	Confirm the exact race window: Walk through the doc cache write path step-by-step — when is doc_id.pdf written vs doc_id.meta.json? Are they written sequentially, and is there a gap between them where a concurrent read from another worker process could see a partial/inconsistent state (e.g., .pdf exists but .meta.json doesn’t yet, or vice versa)?
	2.	Confirm whether this matches the observed symptom: We see ~2 intermittent 404s out of ~80 sequential /positions calls for a single 79-page document, all using the same doc_id. Does the write-then-read race you identified actually explain isolated, sparse 404s like this — or is there a more likely alternative explanation (e.g., something in _resolve_pdf, TTL cleanup thread timing, or something else in the read path at server.py:441/446/449)?
	3.	What is the actual blast radius if this race happens in production? Could it ever return wrong/stale data to the user (silent data corruption) rather than just a 404, given this involves audit documents with redaction workflows?
	4.	What is the minimal, safest fix — cross-process file lock (portalocker) around the doc cache write path, atomic write via temp-file-then-rename (like session store already does at _session_store.py:343/350), or something else? Please don’t implement yet — just lay out the options and tradeoffs (performance impact of locking on every read given high request volume, complexity, risk of introducing deadlocks with the existing per-process threading.Lock).
	5.	Separately: please also assess the risk of the no global exception handler situation — are there other endpoints besides /positions where an unhandled exception could crash a worker or return a bare connection-reset instead of a clean error, given what we saw earlier (curl showing “underlying connection was closed”)?