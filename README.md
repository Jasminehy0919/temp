I think the recurring “Out of Memory” crashes started after Solution 1 (omitting pdf_b64 on cache hit), not the token-gating fix. Solution 1 relies on the frontend caching decoded PDF content keyed by doc_id so it can serve cache hits without re-receiving pdf_b64. Show me where this frontend cache lives, and answer directly: does it have any size limit, eviction policy (LRU or similar), or cleanup when a doc/tab is no longer in use? If not, this cache likely grows unbounded as more files are opened across a session, which would explain increasing memory usage over time leading to the crash.