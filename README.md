I’m now seeing recurring “Aw, Snap! Out of Memory” crashes after the token-gating fix. Before assuming it’s unrelated, check: when a switch is discarded due to a stale token (in any of the 6 guard checks), does the in-flight work that already started (especially #handle.loadFromBytes(...) inside _loadSlotPdf, and any PDF decode/render buffers) get aborted/cancelled and its memory released? Or does it keep running to completion and get held in memory even though its result is discarded? Also check whether #handle or any PDF render buffers from discarded switches are still referenced anywhere (e.g., in a cache) preventing garbage collection.