Go ahead and implement Plan A: add stale checks at intermediate points in the decode path with early return, and explicitly null out the large locals (raw string, Uint8Array buffer, bytes variable) on every stale-return path. Add the final cleanup block in _loadSlotPdf to drop local refs regardless of success/fail. Don’t touch the cancellation/AbortController approach (Plan B) — just the memory-release-on-stale-detection part.

After implementing, list every stale-return path you touched and confirm what gets nulled at each one.