This makes sense — the root cause is discarded switches leaving partial PDF decode work in memory instead of being aborted. Before implementing, tell me: can atob() and the pdf.js parsing inside _loadSlotPdf actually be aborted mid-flight (e.g., via AbortController/AbortSignal), or can you only skip committing the result after it finishes? If decoding can’t be interrupted, what’s your plan to at least free the large intermediate buffers/strings as soon as a switch is detected as stale, instead of waiting for the next successful switch’s setDocId()/reset()?